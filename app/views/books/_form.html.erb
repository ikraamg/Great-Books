<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= form.label "Categories of the Story" %><br />
  <%= form.collection_check_boxes :category_ids, @categories, :id, :name do |b| %>
    <div class="d-flex>
      <%= b.label %>
      <%= b.check_box  %>
    </div>
  <% end %>
</div>

  <div class="field">
    <%= form.label :title_of_story %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :story_body %>
    <%= form.text_area :text %>
  </div>
  
  <div class="field">
    <%= form.label :feaured_story_image %>
    <%= form.file_field :photo %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
